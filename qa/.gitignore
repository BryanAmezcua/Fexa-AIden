node_modules/
.env
.env.local
.env.qa
.env.*.local

# Auth state files contain session cookies — never commit
auth/*.json

# Playwright artifacts
test-results/
playwright-report/
blob-report/
playwright/.cache/

# OS noise
.DS_Store
/exploration/

# Reports — generated QA artifacts (per-ticket HTML reports, seed
# manifests, timestamped archives). All regenerated per run; not tracked.
/reports/
